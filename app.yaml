command:
  - bash
  - -lc
  - |
      cd frontend
      npm install
      npm run build
      cd ..
      uvicorn backend.app.main:app --host 0.0.0.0 --port 8000

env:
  - name: PYTHONUNBUFFERED
    value: "1"
  - name: PORT
    value: "8000"